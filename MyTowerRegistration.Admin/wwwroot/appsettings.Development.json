{
  // Development overrides — loaded automatically when ASPNETCORE_ENVIRONMENT=Development.
  // Overrides appsettings.json for local development.
  // Also public — but this only runs locally, so the localhost URL is harmless.
  "ApiBaseUrl": "http://localhost:5026"
}
