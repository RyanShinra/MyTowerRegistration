{
  // Production configuration — served as a static file downloaded by the browser.
  //
  // IMPORTANT: This file is PUBLIC. The browser downloads it as a plain text file.
  // Never put secrets, passwords, connection strings, or API keys here.
  // The API URL is safe to expose (it's a public endpoint by definition).
  "ApiBaseUrl": "https://io.mytower.dev"
}
